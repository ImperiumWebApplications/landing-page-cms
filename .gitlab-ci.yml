variables:
  BUILD_ARTIFACT: "build-artifact"

stages:
  - check-and-test
  - prettier-frontend
  - build-frontend
  - deploy-frontend
  - prettier-backend
  - build-backend
  - deploy-backend

include:
  - '/check-and-test.yml'
  - '/frontend-staging-deployment.yml'
  - '/backend-staging-deployment.yml'
  - '/frontend-production-deployment.yml'
  - '/backend-production-deployment.yml'